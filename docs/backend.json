
{
  "entities": {
    "UserProfile": {
      "title": "Kullanıcı Profili",
      "description": "Öğrenci, Öğretmen ve Yönetici bilgilerini, rollerini ve hedef sınavlarını saklar.",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "email": { "type": "string", "format": "email" },
        "displayName": { "type": "string" },
        "role": { "type": "string", "enum": ["student", "teacher", "school_admin", "admin"] },
        "targetExam": { "type": "string", "description": "Kullanıcının hazırlandığı sınav (LGS, YKS, KPSS vb.)" },
        "coachId": { "type": "string", "description": "Öğrenciye atanan koçun UID'si." },
        "activationCode": { "type": "string", "description": "Öğretmenler için benzersiz aktivasyon kodu." },
        "school": { "type": "string" },
        "grade": { "type": "string" },
        "branch": { "type": "string", "description": "Öğretmenin uzmanlık alanı veya öğrencinin bölümü." },
        "bio": { "type": "string", "description": "Uzman hakkında kısa bilgi." },
        "experience": { "type": "number", "description": "Deneyim yılı." },
        "rating": { "type": "number", "description": "Ortalama başarı puanı." },
        "reviewCount": { "type": "number", "description": "Toplam değerlendirme sayısı." },
        "badges": { "type": "array", "items": { "type": "string" }, "description": "Rozetler: verified, diploma, premium vb." },
        "coachType": { "type": "string", "description": "Akademik, Sınav, Motivasyon vb." },
        "meetingType": { "type": "string", "enum": ["online", "physical", "both"] },
        "photoUrl": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["uid", "email", "role"]
    },
    "CoachingSession": {
      "title": "Koçluk Seansı",
      "description": "Koç ve öğrenci arasındaki planlanmış görüşmeler.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "teacherId": { "type": "string" },
        "scheduledAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["scheduled", "completed", "cancelled"] },
        "notes": { "type": "string" }
      },
      "required": ["studentId", "teacherId", "scheduledAt"]
    },
    "Task": {
      "title": "Görev",
      "description": "Koç tarafından öğrenciye atanan hedefler.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "teacherId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "dueDate": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["pending", "completed"] }
      },
      "required": ["studentId", "title", "status"]
    },
    "ConnectionRequest": {
      "title": "Bağlantı İsteği",
      "description": "Öğrenci ve öğretmen arasındaki eşleşme talebi.",
      "type": "object",
      "properties": {
        "studentId": { "type": "string" },
        "teacherId": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "accepted", "rejected"] },
        "message": { "type": "string" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["studentId", "teacherId", "status"]
    }
  },
  "auth": {
    "providers": ["email", "google"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Kullanıcılar bu koleksiyonda saklanır."
    },
    "/sessions/{sessionId}": {
      "schema": "CoachingSession",
      "description": "Görüşme kayıtları."
    },
    "/tasks/{taskId}": {
      "schema": "Task",
      "description": "Öğrenci görevleri."
    },
    "/requests/{requestId}": {
      "schema": "ConnectionRequest",
      "description": "Bağlantı talepleri."
    }
  }
}
